First release candidate.

- Engine: DeepSeek Harness 0.1.5-rc.2, Android native addons cross-compiled.
- Approvals can be reopened after dismissal, so turns no longer get stuck.
- Failed turns show the engine's reason instead of stopping silently.
- Fixed: approval buttons no longer stop the turn; per-session stats no
  longer carry over; parallel tool timings; drawer running state; sorting.
- Security: encrypted key storage, backups off, localhost-only network.
