# DSHA 版本与发布工作流

> 本文件是**团队约定**，每次发布都按此执行。最后更新：见 git log。

---

## 1. 版本号规则

### 1.1 谁决定什么

| 部分 | 形式 | 谁决定 |
|---|---|---|
| **大版本号** `X.Y.Z` | `0.1.0` / `0.1.1` / `0.2.0` … | **仅由项目所有者决定**。只有所有者说"升级大版本号"时才动；升级后 `rcN` 从 `1` 重新开始 |
| **候选序号** `rcN` | `rc1`、`rc2`、`rc3` … | AI/开发者**只能**递增这一部分 |

> 当前大版本号：**`0.1.0`**（在所有者宣布升级前，本地与推送都只在这个号上递增 rc）

### 1.2 三个位置上的版本

| 位置 | 形式 | 说明 |
|---|---|---|
| **本地开发版** | `0.1.0-rc1` → `0.1.0-rc2` → … | 本地每完成一批改动就递增 `rcN`；`versionName` 同步为 `0.1.0-rcN` |
| **GitHub 推送版** | `0.1.0-rcX` | **与本地同一个大版本号**，只是推的是当时那个 `rcN`。推送**不会**让大版本号前进 |
| **正式 Release** | `0.1.0`（无 rc 后缀） | 所有者确认最终版时才去掉 rc 后缀 |

**大版本号升级的唯一触发条件**：所有者明确说"升到 X.Y.Z"。此时：
1. 大版本号改为新的 `X.Y.Z`
2. `rcN` 归 `1`（成为 `X.Y.Z-rc1`）
3. 后续仍由开发者递增 rc

### 1.3 `versionCode`（Android 要求，必须单调递增）

- `versionName` 是给人看的，`versionCode` 是给系统比较的：**F-Droid 与 Android 都要求它只增不减**。
- 约定：**每次发布 +1**（当前为 `1`）。本地构建不必每次改，发布前必须改。
- 建议编码规则（便于人肉核对）：`versionCode = major*10000 + minor*100 + patch*10 + rcN`
  例：`0.1.1-rc2` → `112`；`0.2.0` → `2000`。若嫌麻烦，直接逐次 +1 也可接受。

---

## 2. 提交与推送节奏

```
本地：随时 commit（可以很多次，含试验性提交一起攒着）
        ↓
所有者决定"要推 APK 了"
        ↓
【前置门槛】完成一次大的三项审查：代码 / 界面 / 安全
        ↓
确认通过后：push 代码 + 建立对应 Release（并放 APK）
```

**硬规则**
- ❌ **不要在每次小改动后推 GitHub**。GitHub 只在**所有者决定推 APK** 时才同步。
- ❌ **不要把 APK 提交进仓库**（`.gitignore` 已排除 `*.apk`）；仓库只放源码与随包引擎资产。
- ✅ 本地提交信息要清楚（每批改动都写清做了什么、怎么验证的），因为推送时是批量上云。
- ✅ 推送前自查：构建通过 + 真机跑过一轮对话 + 工作区干净。

### 2.1 推送前的三项审查门槛（**必须完成，不可跳过**）

| 审查 | 覆盖内容 | 产出 |
|---|---|---|
| **代码审查** | 正确性、并发/锁、生命周期、资源释放、死代码 | 真实缺陷清单 + 修复记录 |
| **界面审查** | 操作逻辑、状态一致性、数字显示正确性、空态/错误态是否有出口、文案与行为是否一致 | 同上（**需要真机解锁做界面级验证**） |
| **安全审查** | 密钥与凭据、组件导出、外部输入处理、网络与明文、日志泄露、权限、备份 | 同上 |

三项审查**各自独立**（不同角度审视同一份代码），审查发现的问题**修完并验证**后才算通过。
审查结论需如实汇报：通过项、发现项、未解决项、以及**已知限制**一并向所有者交代。

> ⚠️ 界面审查依赖真机解锁（当前设备长期处于安全锁屏，`screencap` 全黑、`uiautomator dump` 为空）。
> 若届时仍无法解锁，必须**明确告知所有者"界面级验证未完成"**，不得默认通过。

---

## 3. GitHub Release 规则

1. **依次追加，不覆盖**：每个推送版本都建一个**新的** Release 与 tag（`v0.1.1-rc1`、`v0.1.1-rc2` …）。
   ❌ 不允许"删掉旧 Release 再传新的"（此前我就是这么做的，属错误做法，已停止）。
2. **历史 Release 一律保留**，便于回溯与对比。
3. **只有最终版是正式 Release**：rc 版本一律勾选 **Pre-release**；所有者确认的最终版才是正式 release。
4. **tag 命名**：`v<versionName>`，例如 `v0.1.1-rc1`。使用**附注标签**（`git tag -a`），带说明。
5. **绝不清空仓库**：Release 的增删与仓库内容无关，任何时候都不得为了"重发"而清库或重写历史。

---

## 4. 最终形态（目标）

| 目标 | 说明 |
|---|---|
| **GitHub 只有源码** | 仓库不含任何构建产物 |
| **F-Droid 上线** | 由 F-Droid 编译并**用 F-Droid 的密钥签名** |
| **GitHub Release 的 APK = F-Droid 编译并打标签的版本** | 即：Release 附件应来自 F-Droid 构建产物，而非我们本地签名包 |

**这意味着一个必须提前知道的后果**：F-Droid 用自己的密钥签名，与我们本地开发密钥**不是同一把**。
Android 不允许签名不同的应用互相覆盖安装 —— 所以：

- 已经侧载我们本地包的设备，**无法直接升级**到 F-Droid 版，必须卸载重装（数据会丢）。
- 因此建议：**尽早决定主分发渠道**。若确定走 F-Droid，就应尽快停止分发本地签名包，
  避免用户装了一个"以后升不上去"的版本。

---

## 5. F-Droid 上线的硬性要求 与 当前差距（**最重要的部分**）

F-Droid 不是"传个 APK 上去"，它的收录有明确政策。逐条对照我们当前状态：

| # | F-Droid 要求 | 我们的现状 | 差距 / 结论 |
|---|---|---|---|
| 1 | **必须能从源码构建，且不允许预编译二进制** | 随包 `node` 可执行文件、`pty.node`/`system.node` 等原生件、整个 `node_modules` 都是**预编译产物** | 🔴 **最大障碍**。F-Droid 政策原则上不接受预编译二进制。可选路线见 §5.1 |
| 2 | 许可证必须是自由软件 | 本项目 AGPL-3.0；上游引擎 MIT ✅ | ✅ 满足（第三方清单见 NOTICE.md） |
| 3 | 需声明 Anti-Feature（如依赖非自由网络服务） | 应用依赖 DeepSeek 官方 API（非自由服务） | ⚠️ 需在 metadata 里声明 `NonFreeNet`（**不阻止收录**，但必须如实标注） |
| 4 | `versionCode` 单调递增、`versionName` 与 tag 对应 | 当前 `versionCode=1`、`versionName=0.1` | ⚠️ 需按 §1.3 规范化 |
| 5 | 元数据：`fdroiddata` 提交 `metadata/<appid>.yml` | 尚未提交 | ⚠️ 待办（见 §5.2） |
| 6 | 应用不得依赖不可复现的外部下载 | 首次启动会**解包内置资产**（不是下载） | ✅ 可接受（全部资产随包） |
| 7 | `targetSdk` 需为较新版本 | 我们**刻意** `targetSdk = 28`（为保留从数据目录执行二进制的 SELinux 权限） | ⚠️ **需核实 F-Droid 当前对 targetSdk 的要求**；这是设计取舍与平台政策的正面冲突，若无法满足需评估替代方案（proot/bubblewrap 路线） |
| 8 | 应用内不得有广告/追踪 | 无广告、无第三方统计 ✅（`@opentelemetry` 是引擎自带的本地埋点，不外发） | ✅ 满足 |

### 5.1 关于"不允许预编译二进制"的可选路线

| 路线 | 做法 | 代价 |
|---|---|---|
| **A. 全部源码构建**（最正统） | 在 F-Droid 构建配方里：交叉编译 Node.js for Android → `npm install` 时交叉编译原生件 → 构建 APK | 🔴 工作量很大（Node 交叉编译 + 每个原生件都要能编）；构建时间长，F-Droid 构建机需装 NDK |
| **B. 申请例外** | 向 F-Droid 说明"随包引擎是上游 MIT 项目的发行物" | ⚠️ 成功率低；政策对二进制很严 |
| **C. 拆分发行** | F-Droid 收录"不随包引擎"的版本（首次启动下载引擎） | ⚠️ 与"开箱即用、离线可用"的产品定位冲突 |
| **D. 暂缓 F-Droid** | 先把 GitHub 作为主渠道做扎实，F-Droid 作为长期目标 | ✅ 现实、低风险 |

> 我的建议：**先按 D 推进产品，同时并行验证 A 的可行性**（先做一次 Node for Android 的交叉编译可行性验证，
> 确认能编出来再投入完整配方）。在 A 验证通过前，不要向用户承诺 F-Droid 上线时间。

### 5.2 F-Droid 提交流程（待执行）

1. 在 `fdroiddata` 仓库提交 `metadata/dev.dsh.host.yml`，关键字段：
   `Categories` / `License` / `SourceCode` / `IssueTracker` / `Builds`（versionName、versionCode、commit）/ `AutoUpdateMode` / `UpdateCheckMode: Tags` / `AntiFeatures: NonFreeNet`
2. 向 F-Droid 提交 Merge Request，等待审核（可能来回数轮）
3. 审核通过后，F-Droid 侧构建并签名；我们据此在 GitHub 建对应的 Release 与 tag

---

## 6. 每次发布的检查清单

**门槛（必须先全部完成）**
- [ ] 所有者已明确说"要推 APK"
- [ ] **代码审查**完成，发现的问题已修并验证
- [ ] **界面审查**完成（含真机界面级验证；若无法解锁则已如实告知未完成）
- [ ] **安全审查**完成，发现的问题已修并验证
- [ ] 三项审查的结论（通过项 / 发现项 / 未解决项 / 已知限制）已向所有者汇报

**发布动作**
- [ ] `versionName` = `<当前大版本号>-rcN`；`versionCode` 已递增
- [ ] `CHANGELOG.md` 增加本版本条目（写清改动、实测证据、已知限制）
- [ ] 构建通过：`gradle assembleRelease`
- [ ] 真机验证：全新安装 → 引擎启动 → 完成一轮对话 → 无崩溃
- [ ] 工作区干净（`git status` 无残留）
- [ ] 本地提交完成
- [ ] `git push`
- [ ] 建 Release + tag `v<versionName>`，**勾选 Pre-release**（正式版除外）
- [ ] 若已有 F-Droid 版：Release 附件用 F-Droid 构建产物；否则暂不放 APK 或标注来源
- [ ] 不改动、不删除任何历史 Release

---

## 7. 当前状态的迁移说明

| 项 | 现状 | 迁移动作 |
|---|---|---|
| `versionName` | `0.1` | 下次发布改为规范形式（如 `0.1.1-rc1`） |
| `versionCode` | `1` | 按 §1.3 递增 |
| 已发布的 tag | `dsha-0.1` | **保留不动**（不删除、不重写）。下次起改用 `v<versionName>` 命名 |
| 已发布的 Release | 名为 `DSHA 0.1`，附件是本地签名 APK | **保留不动**。其中 APK 的来源（本地签名）在正文里已标注；未来 F-Droid 版会新建 Release |
| 主渠道 | GitHub（本地签名 APK） | 待所有者决定是否切换为 F-Droid 主渠道（切换会带来"需卸载重装"的影响，见 §4） |

---

## 8. 我（AI）承诺遵守的纪律

1. **本地随时提交**，但**不擅自 push**。推送的唯一触发条件是"所有者说要推 APK"。
2. **不擅自改大版本号**：只有所有者宣布升级时才改，且改后 `rcN` 归 1；平时只递增 `rcN`。
3. **推送前必须完成代码 / 界面 / 安全三项审查**，并把结论如实汇报（含未解决项与已知限制）；
   **界面级验证做不了就说做不了**，不带过。
4. **不删除、不覆盖历史 Release**；每个版本新建 Release + tag。
5. **不把 APK 提交进仓库**。
6. 每次推送前给出：本批改动清单、真机验证证据、已知限制。
7. 凡涉及"发布/上线"的判断（尤其 F-Droid 政策），如实报告差距与风险，不为了让流程好看而省略不利信息。
