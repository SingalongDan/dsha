# DSHA 版本与发布工作流

> 本文件是**团队约定**，每次发布都按此执行。最后更新：见 git log。

---

## 1. 版本号规则

### 1.1 谁决定什么

| 部分 | 形式 | 谁决定 |
|---|---|---|
| **大版本号** `X.Y.Z` | `0.1.0` / `0.1.1` / `0.2.0` … | **仅由项目所有者决定**。只有所有者说"升级大版本号"时才动；升级后 `rcN` 从 `1` 重新开始 |
| **候选序号** `rcN` | `rc1`、`rc2`、`rc3` … | AI/开发者**只能**递增这一部分 |

> 当前大版本号：**`0.1.0`**（在所有者宣布升级前，本地与推送都只在这个号上递增 rc）

### 1.2 三个位置上的版本

| 位置 | 形式 | 说明 |
|---|---|---|
| **本地开发版** | `0.1.0-rc1` → `0.1.0-rc2` → … | 本地每完成一批改动就递增 `rcN`；`versionName` 同步为 `0.1.0-rcN` |
| **GitHub 推送版** | `0.1.0-rcX` | **与本地同一个大版本号**，只是推的是当时那个 `rcN`。推送**不会**让大版本号前进 |
| **正式 Release** | `0.1.0`（无 rc 后缀） | 所有者确认最终版时才去掉 rc 后缀 |

**大版本号升级的唯一触发条件**：所有者明确说"升到 X.Y.Z"。此时：
1. 大版本号改为新的 `X.Y.Z`
2. `rcN` 归 `1`（成为 `X.Y.Z-rc1`）
3. 后续仍由开发者递增 rc

### 1.3 `versionCode`（Android 要求，必须单调递增）

- `versionName` 是给人看的，`versionCode` 是给系统比较的：**F-Droid 与 Android 都要求它只增不减**。
- 约定：**每次发布 +1**（当前为 `1`）。本地构建不必每次改，发布前必须改。
- 建议编码规则（便于人肉核对）：`versionCode = major*10000 + minor*100 + patch*10 + rcN`
  例：`0.1.1-rc2` → `112`；`0.2.0` → `2000`。若嫌麻烦，直接逐次 +1 也可接受。

---

## 2. 提交与推送节奏

```
本地：随时 commit（可以很多次，含试验性提交一起攒着）
        ↓
所有者决定"要推 APK 了"
        ↓
【前置门槛】完成一次大的三项审查：代码 / 界面 / 安全
        ↓
确认通过后：push 代码 + 建立对应 Release（并放 APK）
```

**硬规则**
- ❌ **不要在每次小改动后推 GitHub**。GitHub 只在**所有者决定推 APK** 时才同步。
- ❌ **不要把 APK 提交进仓库**（`.gitignore` 已排除 `*.apk`）；仓库只放源码与随包引擎资产。
- ✅ 本地提交信息要清楚（每批改动都写清做了什么、怎么验证的），因为推送时是批量上云。
- ✅ 推送前自查：构建通过 + 真机跑过一轮对话 + 工作区干净。

### 2.1 推送前的三项审查门槛（**必须完成，不可跳过**）

| 审查 | 覆盖内容 | 产出 |
|---|---|---|
| **代码审查** | 正确性、并发/锁、生命周期、资源释放、死代码 | 真实缺陷清单 + 修复记录 |
| **界面审查** | 操作逻辑、状态一致性、数字显示正确性、空态/错误态是否有出口、文案与行为是否一致 | 同上（**需要真机解锁做界面级验证**） |
| **安全审查** | 密钥与凭据、组件导出、外部输入处理、网络与明文、日志泄露、权限、备份 | 同上 |

三项审查**各自独立**（不同角度审视同一份代码），审查发现的问题**修完并验证**后才算通过。
审查结论需如实汇报：通过项、发现项、未解决项、以及**已知限制**一并向所有者交代。

> ⚠️ 界面审查依赖真机解锁（当前设备长期处于安全锁屏，`screencap` 全黑、`uiautomator dump` 为空）。
> 若届时仍无法解锁，必须**明确告知所有者"界面级验证未完成"**，不得默认通过。

---

## 3. GitHub Release 规则

1. **依次追加，不覆盖**：每个推送版本都建一个**新的** Release 与 tag（`v0.1.1-rc1`、`v0.1.1-rc2` …）。
   ❌ 不允许"删掉旧 Release 再传新的"（此前我就是这么做的，属错误做法，已停止）。
2. **历史 Release 一律保留**，便于回溯与对比。
3. **只有最终版是正式 Release**：rc 版本一律勾选 **Pre-release**；所有者确认的最终版才是正式 release。
4. **tag 命名**：`v<versionName>`，例如 `v0.1.1-rc1`。使用**附注标签**（`git tag -a`），带说明。
5. **绝不清空仓库**：Release 的增删与仓库内容无关，任何时候都不得为了"重发"而清库或重写历史。

---

## 4. 最终形态（目标）

| 目标 | 说明 |
|---|---|
| **GitHub 只有源码** | 仓库不含任何构建产物 |
| **F-Droid 上线** | 由 F-Droid 编译并**用 F-Droid 的密钥签名** |
| **GitHub Release 的 APK = F-Droid 编译并打标签的版本** | 即：Release 附件应来自 F-Droid 构建产物，而非我们本地签名包 |

**这意味着一个必须提前知道的后果**：F-Droid 用自己的密钥签名，与我们本地开发密钥**不是同一把**。
Android 不允许签名不同的应用互相覆盖安装 —— 所以：

- 已经侧载我们本地包的设备，**无法直接升级**到 F-Droid 版，必须卸载重装（数据会丢）。
- 因此建议：**尽早决定主分发渠道**。若确定走 F-Droid，就应尽快停止分发本地签名包，
  避免用户装了一个"以后升不上去"的版本。

---

## 5. F-Droid 上线的硬性要求 与 当前差距（**最重要的部分**）

> **结论先行（研究结论）**：F-Droid **主仓库在现有形态下事实上不可行**，应作为 **nice-to-have 而非 must-have**。
> 现实的分发基本盘是 **GitHub Release / Obtainium**；F-Droid 作为中长期目标，路径是"源码构建配方 **+** Reproducible Builds"。
> 完整研究报告见 `DSHA-FDroid-研究报告.md`（每条结论附来源链接，并列出 12 条"未查到确证"）。

### 5.0 三条硬阻塞（为什么"随包引擎"过不了 F-Droid 主仓库）

| # | 阻塞 | 依据 |
|---|---|---|
| 1 | **依赖来源不在白名单** | 政策要求二进制依赖来自**源码编译**或白名单。白名单里 "Node.js" 看似可用，但 nodejs.org 官方制品是 **glibc 链接且没有 android 目标** → 对 bionic **无效**；"Debian repository downloads" 同理（Debian 的 `nodejs` 也是 glibc） |
| 2 | **仓库内提交了预编译件** | `assets/usr/` 直接入库：`node` 47 MB、`libicudata.so.78` 33 MB、完整 `node_modules` |
| 3 | **构建机产不出这些文件** | 仓库原本**没有任何原生件构建链路**（无 `ndkVersion` / `externalNativeBuild`）→ 已补 `host-app/scripts/build-native-addons.ps1`（§5.3），但**整套 bionic Node + 依赖库**仍需在构建机里从源码编出来 |

**最硬的一条事实**：**bionic 版 Node 没有任何现成合规制品可下载** —— 全世界只有 Termux 在持续产出，
而 **Termux 官方明确要求不得复用其包仓库**：
`You cannot use our package repositories in your own project(s). Please build packages and host them yourself.`
（许可证允许复用其组件，但**仓库托管被明确排除**。）

### 5.2 ⚠️ "只上 F-Droid"是一扇单向门

F-Droid 用**它自己的密钥**签名，那个签名就成为应用身份。因此：

- 一旦 F-Droid 发布了 APK，**我们再也无法给这些用户分发自己签名的包**（Android 拒绝签名不同的覆盖安装）；
- （**本项已不适用**：所有者确认只走 F-Droid 单一渠道，因此不需要 RB。以下保留供将来若重开自签分发时参考。）`n- 想在两边都发，唯一正规解法是 **Reproducible Builds**：让 F-Droid 能构建出与上游**逐字节相同**的 APK，
  从而**分发我们自己签名的包** —— 这样两条渠道签名一致、可交叉更新。
  **RB 不是"预编译二进制的后门"**（研究员已核），它要求 F-Droid 自己也能从源码构建出相同产物。
- 若决定**纯 F-Droid**：**不需要生成我们自己的密钥**（用 F-Droid 的即可），但代价是**放弃了自建分发的能力**，
  且在上架等待期（周到月级）**没有任何分发渠道**。

### 5.3 已完成的准备工作

| 项 | 状态 |
|---|---|
| gradle wrapper | ✅ 已补（8.13）—— F-Droid 配方默认走 wrapper |
| 依赖仓库官方优先 | ✅ 默认只有 `google()` + `mavenCentral()`；阿里云镜像退到 `DSH_CN_MIRROR=1` 开关（**实测：国内直连 Maven Central 会失败，本地构建必须用开关**；F-Droid/境外 CI 走官方源） |
| 第三方许可清单 | ✅ `NOTICE.md`（扫描 490 个随包依赖；唯一 copyleft 是 `@img/sharp-wasm32` 的 LGPL 组件） |
| 上游许可口径 | ✅ 已查实**上游是 MIT**（据随包 `LICENSE` 正文 + `package.json`），此前的"AGPL"表述是错的，已更正 |
| 原生件构建链路 | ✅ `host-app/scripts/build-native-addons.ps1`（从随包源码编译，`system.node` **逐字节复现**、`pty.node` 功能验证通过） |
| 版本口径 | ✅ CHANGELOG 改为 `0.1.0-rcN` 写法并记录随包引擎版本 |

### 5.3.1 ⚠️ 16KB 页大小：真实存在的功能缺陷（与 targetSdk 无关）

**普查结果**（对随 APK 分发的全部 ELF 做 LOAD 段对齐检查，阈值 16384）：

| 范围 | 结果 |
|---|---|
| `node` 本体 | ✅ 已对齐（0x4000+） |
| node 实际加载的 8 个库（icu×3 / ssl / crypto / cares / sqlite / libc++_shared） | ✅ 已对齐 |
| 本项目自编的 `pty.node` / `system.node` | ✅ 已对齐（**曾经是 0x1000，已修**：链接加 `-Wl,-z,max-page-size=16384`） |
| Termux 前缀内 **74 个 ELF 中的 50 个** | ❌ **未对齐 —— 含 `bash` 本身与全部 coreutils**（`cat`/`cp`/`chmod`/`chown`/`awk`/`cut`/`date`/`diff`…） |
| 依赖未对齐库的文件数 | `libandroid-selinux.so` ← 37 个；`libgmp.so` ← 39 个；`libmpfr.so` ← 1（awk）；`libprocps.so` ← 1（ps） |

**后果**：在 16KB 页设备上，引擎能启动（node 已对齐），但 **agent 一执行 shell 命令就会失败**（`bash` 自己加载不了）。

**根因与路径**：
- 根因：随包的是**旧版 Termux bootstrap**，其构建基线为 4KB。
- 上游状态：修复在 **Termux master**，**最新 release（v0.119.0-beta.3）尚未包含**
  （Termux 官方 F-Droid 版同样因此在新设备上跑不起来）。
- **可选路径**：① **等 Termux 发布已对齐的 bootstrap 后刷新我们的前缀**（推荐的维护任务，非重写）；
  ② 自行重编 bash + ~40 个 coreutils + `libandroid-selinux` + `libgmp`（工作量大，暂不采用）。
- ⚠️ **未闭环**：本机是 **4KB 页设备**，**无法在 16KB 环境实测**；且 Termux 跟踪 issue（#4185）
  的最新状态**未完整核到**（页面抓取被截断），需后续复核。

**与 targetSdk 的关系**：**完全无关**。它是设备/ABI 层面的问题，所以"保持 targetSdk=28"并不能回避它。
### 5.3.2 版本标签规范（F-Droid 要求）

F-Droid 快速入门指南原文：**"应用的上游 Git 存储库中的每个官方 release 提交都应该有一个标签。
比如，如果 AndroidManifest 包含 `versionName: '1.0'`，则该提交需要一个 `v1.0` 标签。"**

**我们的约定**：`v<versionName>`，即 `v0.1.0-rc1`、`v0.1.0-rc2`…（与 `versionName` 完全一致，避免歧义）。
使用**附注标签**（`git tag -a`），带说明便于追溯。

⚠️ **一个待验证的注意点（rc 后缀与自动更新）**：
F-Droid 的 `UpdateCheckMode: Tags` 按正则筛选标签，其默认模式通常期望 `v1.2.3` 这种纯数字形式，
**`-rc1` 这类后缀可能不被默认正则接受**。因此：
- 首次提交时建议**先不开启自动更新**（用 `UpdateCheckMode: None` 手动更），
  等确认标签正则行为后再切换；
- 或为该应用显式配置 `UpdateCheckData` / 调整匹配规则。
（此项**未实测**，因为需要真正提交到 fdroiddata 后在 CI 里验证。）

**历史标签**：`dsha-0.1`（早期命名，指向旧提交）**保留不删**，仅作为历史记录；
自 `v0.1.0-rc1` 起按上述规范命名。
### 5.4 待办（按优先级）

1. ~~APK 瘦身~~ **降级**：不再是上架门槛（只走 F-Droid），保留为**用户体验优化**（122 MiB 对侧载/存储仍偏大）
2. **发 fdroiddata 政策咨询 issue**（**先问再写代码，不要直接提 MR**）：说明我们"随包 Node 运行时"的形态，
   请其确认可行的配方写法与构建资源限制（`timeout` / CPU / 内存）——这是决定路线可行性的关键一问
3. **自编译原生件的 POSIX shell 版本**：F-Droid 构建机是 Linux，需要 `.sh` 版本；
   ⚠️ **本机无 bash，无法验证**，须在 F-Droid 阶段实测（当前 `.ps1` 已实测可用）
4. **`targetSdk = 28` 的冲突评估**：这是我们的刻意选择（保留从数据目录执行二进制的 SELinux 权限），
   与平台/F-Droid 对较新 targetSdk 的期待正面冲突，需评估替代方案（proot/bubblewrap 路线）
5. 若走 F-Droid：**bionic Node 的源码构建配方**（Termux 自述约 2 小时，可能顶满 F-Droid 默认 `timeout: 7200`，
   且构建 VM 默认仅 1 CPU / 2048 MB —— 超时风险高）

### 5.1 关于"不允许预编译二进制"的可选路线

| # | F-Droid 要求 | 我们的现状 | 差距 / 结论 |
|---|---|---|---|
| 1 | **必须能从源码构建，且不允许预编译二进制** | 随包 `node` 可执行文件、`pty.node`/`system.node` 等原生件、整个 `node_modules` 都是**预编译产物** | 🔴 **最大障碍**。F-Droid 政策原则上不接受预编译二进制。可选路线见 §5.1 |
| 2 | 许可证必须是自由软件 | 本项目 **AGPL-3.0-only**；上游引擎 MIT ✅ | ✅ 满足（第三方清单见 NOTICE.md） |
| 3 | 需声明 Anti-Feature（如依赖非自由网络服务） | 应用依赖 DeepSeek 官方 API（非自由服务） | ⚠️ 需在 metadata 里声明 `NonFreeNet`（**不阻止收录**，但必须如实标注） |
| 4 | `versionCode` 单调递增、`versionName` 与 tag 对应 | 当前 `versionCode=1`、`versionName=0.1` | ⚠️ 需按 §1.3 规范化 |
| 5 | 元数据：`fdroiddata` 提交 `metadata/<appid>.yml` | 尚未提交 | ⚠️ 待办（见 §5.2） |
| 6 | 应用不得依赖不可复现的外部下载 | 首次启动会**解包内置资产**（不是下载） | ✅ 可接受（全部资产随包） |
| 7 | `targetSdk` 需为较新版本 | 我们**刻意** `targetSdk = 28`（为保留从数据目录执行二进制的 SELinux 权限） | ⚠️ **需核实 F-Droid 当前对 targetSdk 的要求**；这是设计取舍与平台政策的正面冲突，若无法满足需评估替代方案（proot/bubblewrap 路线） |
| 8 | 应用内不得有广告/追踪 | 无广告、无第三方统计 ✅（`@opentelemetry` 是引擎自带的本地埋点，不外发） | ✅ 满足 |

### 5.1 关于"不允许预编译二进制"的可选路线

| 路线 | 做法 | 代价 |
|---|---|---|
| **A. 全部源码构建**（最正统） | 在 F-Droid 构建配方里：交叉编译 Node.js for Android → `npm install` 时交叉编译原生件 → 构建 APK | 🔴 工作量很大（Node 交叉编译 + 每个原生件都要能编）；构建时间长，F-Droid 构建机需装 NDK |
| **B. 申请例外** | 向 F-Droid 说明"随包引擎是上游 MIT 项目的发行物" | ⚠️ 成功率低；政策对二进制很严 |
| **C. 拆分发行** | F-Droid 收录"不随包引擎"的版本（首次启动下载引擎） | ⚠️ 与"开箱即用、离线可用"的产品定位冲突 |
| **D. 暂缓 F-Droid** | 先把 GitHub 作为主渠道做扎实，F-Droid 作为长期目标 | ✅ 现实、低风险 |

> 我的建议：**先按 D 推进产品，同时并行验证 A 的可行性**（先做一次 Node for Android 的交叉编译可行性验证，
> 确认能编出来再投入完整配方）。在 A 验证通过前，不要向用户承诺 F-Droid 上线时间。

### 5.2 F-Droid 提交流程（待执行）

1. 在 `fdroiddata` 仓库提交 `metadata/io.github.singalongdan.dsha.yml`，关键字段：
   `Categories` / `License` / `SourceCode` / `IssueTracker` / `Builds`（versionName、versionCode、commit）/ `AutoUpdateMode` / `UpdateCheckMode: Tags` / `AntiFeatures: NonFreeNet`
2. 向 F-Droid 提交 Merge Request，等待审核（可能来回数轮）
3. 审核通过后，F-Droid 侧构建并签名；我们据此在 GitHub 建对应的 Release 与 tag

---

## 5.5 分发策略：**只上 F-Droid**（所有者确认；不做 IzzyOnDroid）

| 代号 | 内容 | 状态 |
|---|---|---|
| (c) | 合规清洁：许可证口径、NOTICE、可复现构建、wrapper、仓库顺序 | ✅ **已完成** |
| **(b)** | 自签 + GitHub Release / Obtainium / IzzyOnDroid | ❌ **已放弃**（所有者决定只走 F-Droid） |
| **(a)** | **F-Droid 主仓库**（源码构建配方） | 🎯 **唯一渠道，进行中** |

### 5.5.1 放弃 (b) 与 IzzyOnDroid 带来的减负

| 项 | 变化 |
|---|---|
| **30 MB APK 上限** | ❌ **不再是门槛**（那是 IzzyOnDroid 的规则；F-Droid 主仓库无此限制）。APK 瘦身从"上架门槛"**降级为"用户体验优化"** |
| **AI 政策风险** | ✅ **消失**。F-Droid 现行政策对 AI/LLM 无任何条款（禁令只在 IzzyOnDroid） |
| **Reproducible Builds（3–8 人周）** | ✅ **整个砍掉**。RB 的唯一价值是"让 F-Droid 分发**我们自己签名**的 APK，实现跨渠道更新"；**只有一个渠道就不需要跨渠道** |
| 签名密钥 | ✅ **不需要生成**（F-Droid 自签） |

### 5.5.2 代价：等待期没有任何分发渠道（**必须提前接受**）

F-Droid 流程为 **政策咨询 → 写配方 → 审核**，**周到月级**。此期间：

- ❌ **不能**发 GitHub Release APK —— 一旦自签分发，用户将来就升不到 F-Droid 版（签名不同，Android 拒绝覆盖安装）
- ❌ 没有 Obtainium、没有 IzzyOnDroid
- ✅ 能做的只有：把代码与配方准备好、本地真机自用

> 若等待期不可接受，唯一出路是**重开 (b)**（自签分发）——而那会把 RB 重新拉回计划（+3–8 人周）。
> 这是本策略下最重要的取舍，已由所有者确认承担。

### 5.5.3 剩余的真实工作量（只剩一件，但很重）

**F-Droid 构建机必须从源码编出**：bionic 版 Node（Termux 自述约 **2 小时**）+ 一整套依赖库
（libicu / openssl / c-ares / libc++ / zlib / libffi / sqlite）+ 我们那两个原生插件 + APK。

⚠️ 风险：F-Droid 默认 `timeout: 7200` **已被 node 单步顶满**，构建 VM 默认仅 **1 CPU / 2048 MB**
→ **超时被拒是现实风险**。成本 **2–6 人周**。

⚠️ 另有 `targetSdk = 28` 的冲突未解（我们的刻意选择 vs 平台/F-Droid 对较新 targetSdk 的期待）。

---
## 6. 每次发布的检查清单

**门槛（必须先全部完成）**
- [ ] 所有者已明确说"要推 APK"
- [ ] **代码审查**完成，发现的问题已修并验证
- [ ] **界面审查**完成（含真机界面级验证；若无法解锁则已如实告知未完成）
- [ ] **安全审查**完成，发现的问题已修并验证
- [ ] 三项审查的结论（通过项 / 发现项 / 未解决项 / 已知限制）已向所有者汇报

**发布动作**
- [ ] `versionName` = `<当前大版本号>-rcN`；`versionCode` 已递增
- [ ] `CHANGELOG.md` 增加本版本条目（写清改动、实测证据、已知限制）
- [ ] 构建通过：`gradle assembleRelease`
- [ ] 真机验证：全新安装 → 引擎启动 → 完成一轮对话 → 无崩溃
- [ ] 工作区干净（`git status` 无残留）
- [ ] 本地提交完成
- [ ] `git push`
- [ ] 建 Release + tag `v<versionName>`，**勾选 Pre-release**（正式版除外）
- [ ] 若已有 F-Droid 版：Release 附件用 F-Droid 构建产物；否则暂不放 APK 或标注来源
- [ ] 不改动、不删除任何历史 Release

---

## 7. 当前状态的迁移说明

| 项 | 现状 | 迁移动作 |
|---|---|---|
| `versionName` | `0.1` | 下次发布改为规范形式（如 `0.1.1-rc1`） |
| `versionCode` | `1` | 按 §1.3 递增 |
| 已发布的 tag | `dsha-0.1` | **保留不动**（不删除、不重写）。下次起改用 `v<versionName>` 命名 |
| 已发布的 Release | 名为 `DSHA 0.1`，附件是本地签名 APK | **保留不动**。其中 APK 的来源（本地签名）在正文里已标注；未来 F-Droid 版会新建 Release |
| 主渠道 | GitHub（本地签名 APK） | 待所有者决定是否切换为 F-Droid 主渠道（切换会带来"需卸载重装"的影响，见 §4） |

---

## 8. 我（AI）承诺遵守的纪律

1. **本地随时提交**，但**不擅自 push**。推送的唯一触发条件是"所有者说要推 APK"。
2. **不擅自改大版本号**：只有所有者宣布升级时才改，且改后 `rcN` 归 1；平时只递增 `rcN`。
3. **推送前必须完成代码 / 界面 / 安全三项审查**，并把结论如实汇报（含未解决项与已知限制）；
   **界面级验证做不了就说做不了**，不带过。
4. **不删除、不覆盖历史 Release**；每个版本新建 Release + tag。
5. **不把 APK 提交进仓库**。
6. 每次推送前给出：本批改动清单、真机验证证据、已知限制。
7. 凡涉及"发布/上线"的判断（尤其 F-Droid 政策），如实报告差距与风险，不为了让流程好看而省略不利信息。
