# 复制为 keystore.properties 并填入你自己的密钥（该文件已被 .gitignore 忽略）。
# 也可以用环境变量：DSH_KEYSTORE_FILE / DSH_KEYSTORE_PASSWORD / DSH_KEY_ALIAS / DSH_KEY_PASSWORD
# 未配置时 ssembleRelease 仍可产出**未签名**包，ssembleDebug 完全不受影响。
storeFile=path/to/your-release.jks
storePassword=
keyAlias=
keyPassword=
